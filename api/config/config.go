// Copyright 2020 The Verbis Authors. All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

package config

// Creates a new theme configuration watcher and
// updates the configuration on file changes.
func init() {
	//watcher.AddWatcherFunc(paths.Theme()+Path, func(event *fsnotify.Event) {
	//	logger.Debug("Updating theme configuration file")
	//	Fetch()
	//	err := sockets.Broadcast(cfg)
	//	if err != nil {
	//		logger.WithError(err).Error()
	//	}
	//})
}
